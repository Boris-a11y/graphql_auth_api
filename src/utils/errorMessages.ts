interface errors {
  invalidEmailError: string;
  invalidPasswordError: string;
}

export const errorMessages: errors = {
  invalidEmailError: "The Email doesn't exist",
  invalidPasswordError: "Wrong password",
};
